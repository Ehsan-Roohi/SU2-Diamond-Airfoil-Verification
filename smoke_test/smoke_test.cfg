% Installation smoke test only -- NOT a converged solution.
% Intended wall time: approximately 1--3 minutes on a typical recent laptop;
% actual time is hardware dependent.
SOLVER= EULER
MATH_PROBLEM= DIRECT
RESTART_SOL= NO
READ_BINARY_RESTART= NO

MACH_NUMBER= 3.0
AOA= 0.0
FREESTREAM_TEMPERATURE= 300.0
REF_LENGTH= 1.0
REF_AREA= 1.0
REF_DIMENSIONALIZATION= FREESTREAM_PRESS_EQ_ONE

MARKER_EULER= ( airfoil )
MARKER_FAR= ( farfield )
MARKER_MONITORING= ( airfoil )

NUM_METHOD_GRAD= WEIGHTED_LEAST_SQUARES
CFL_NUMBER= 0.2
ITER= 30
LINEAR_SOLVER= FGMRES
LINEAR_SOLVER_PREC= ILU
LINEAR_SOLVER_ERROR= 1E-8
LINEAR_SOLVER_ITER= 20
CONV_NUM_METHOD_FLOW= ROE
MUSCL_FLOW= NO
ENTROPY_FIX_COEFF= 0.1
TIME_DISCRE_FLOW= EULER_IMPLICIT

MESH_FILENAME= ../meshes/diamond_coarse_480x121.su2
MESH_FORMAT= SU2
TABULAR_FORMAT= CSV
CONV_FILENAME= history_smoke
RESTART_FILENAME= restart_smoke
SCREEN_WRT_FREQ_INNER= 10
OUTPUT_WRT_FREQ= 30
OUTPUT_FILES= (RESTART_ASCII)
SCREEN_OUTPUT= (INNER_ITER, RMS_DENSITY, LIFT, DRAG)
HISTORY_OUTPUT= (ITER, RMS_RES, AERO_COEFF, AERO_COEFF_SURF)
